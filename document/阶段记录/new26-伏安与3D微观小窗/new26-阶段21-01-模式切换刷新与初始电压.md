# 阶段文档21-1-new(26)-模式切换刷新与初始电压

生成时间：2026-06-05  
对应阶段：阶段 A  
提交目标：修复微观动画模式切换即时刷新，并将原理动画区默认外加电压改为 `-1 V`。

## 完成内容

- 在 `PhotoelectricAnimationCanvas.vue` 的动画状态签名中加入 `animationMode`、伏安运动参数、电压参考上限、收集率和电子运动关键数值。
- 修复只切换截止电压/伏安特性模式但其他数值不变时，Canvas watcher 不触发、动画引擎不即时更新的问题。
- 将原理动画区 `DEFAULT_CONTROLS.voltage` 从 `-0.9` 改为 `-1`，使截止电压模式和伏安特性模式初始电压都为 `-1.000 V`。
- 新增 `PhotoelectricAnimationCanvas.spec.js`，覆盖“只改变 `animationMode` 也会向引擎推送新状态”的场景。
- 更新 `PrincipleAnimationSection.spec.js` 中默认电压相关断言。

## 改动文件

- `src/components/animation/PhotoelectricAnimationCanvas.vue`
- `src/components/PrincipleAnimationSection.vue`
- `src/components/__tests__/PhotoelectricAnimationCanvas.spec.js`
- `src/components/__tests__/PrincipleAnimationSection.spec.js`

## 验证结果

已执行：

```bash
npm run test:unit -- --run src/components/__tests__/PrincipleAnimationSection.spec.js src/components/__tests__/ThreeDMicroAnimationPanel.spec.js src/components/__tests__/PhotoelectricAnimationCanvas.spec.js
```

结果：

```text
Test Files  3 passed (3)
Tests       16 passed (16)
```

同时检查：

```bash
rg -n -- "-0\.9" src/components/PrincipleAnimationSection.vue src/components/__tests__/PrincipleAnimationSection.spec.js
```

结果：未发现残留 `-0.9`。

## 未完成 / 风险

- 本阶段只修复刷新链路和原理动画默认电压，尚未改伏安轨迹生命周期、电场线、讲解浮窗或 5 秒收集率。
- 当前未运行全量测试和构建，按计划留到阶段 F 统一执行。

## 下一阶段建议

- 进入阶段 B：将伏安特性模式下的轨迹点生命周期从 `4 s` 缩短为 `1.5 s`，并更新相关轨迹生命周期测试。
