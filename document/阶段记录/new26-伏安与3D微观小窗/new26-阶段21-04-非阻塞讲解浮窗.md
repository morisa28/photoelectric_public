# 阶段文档21-4-new(26)-非阻塞讲解浮窗

生成时间：2026-06-05  
对应阶段：阶段 D  
提交目标：将原理动画区讲解界面改为可拖动、非阻塞浮窗，并保持讲解文本响应式更新。

## 完成内容

- 移除原讲解界面的全屏遮罩结构，不再使用阻塞式 modal。
- 将讲解界面改为固定定位浮窗 `explanation-floating-dialog`。
- 为浮窗 header 增加 pointer 拖拽逻辑，支持在视口安全边距内移动。
- 增加拖拽清理逻辑，在 pointer 结束、取消和组件卸载时移除 window 事件监听。
- 保留 `explanationItems` 的 computed 响应式来源，不把讲解文本固化为打开瞬间的静态内容。
- 更新原理动画区测试，覆盖：
  - 浮窗使用非遮罩结构。
  - 浮窗打开时参数滑块仍可更新动画面板 props。
  - 调整频率后，讲解浮窗文本即时更新。

## 改动文件

- `src/components/PrincipleAnimationSection.vue`
- `src/components/__tests__/PrincipleAnimationSection.spec.js`

## 验证结果

已执行：

```bash
npm run test:unit -- --run src/components/__tests__/PrincipleAnimationSection.spec.js
```

结果：

```text
Test Files  1 passed (1)
Tests       9 passed (9)
```

## 未完成 / 风险

- 本阶段用组件测试验证了非遮罩结构和响应式文本，但没有通过真实浏览器拖拽浮窗。阶段 F 进行视觉或交互验证时应补看浮窗位置和拖动手感。
- 浮窗默认初始位置为固定坐标，会在窄屏下通过样式计算限制到视口内；若后续需要记忆用户位置，可另行添加持久化。

## 下一阶段建议

- 进入阶段 E：将正常电子收集率从滚动窗口即时值改为每 5 秒结算一次，并同步 UI 文案说明电流与收集率的关系。
