# 阶段文档21-6-new(26)-整体验证与视觉检查

生成时间：2026-06-05（Asia/Shanghai）

## 阶段目标

- 对阶段 1 到阶段 5 的代码改动做整体回归验证。
- 运行生产构建，确认项目可构建。
- 运行 3D 场景截图和视觉检查，记录当前视觉基线差异。
- 按用户新增约束，为本阶段生成总结文档并提交 Git。

## 本阶段代码改动

本阶段未修改业务代码。

新增文档：

- `document/阶段记录/new26-伏安与3D微观小窗/new26-阶段21-06-整体验证与视觉检查.md`

## 已验证提交范围

本轮任务相关提交：

- `1e51f9a fix: refresh animation mode and initial voltage`
- `6c271fc fix: shorten volt amp trail lifetime`
- `0191be7 fix: align field line direction and density`
- `bc242c1 feat: add draggable explanation dialog`
- `92ac1f0 fix: settle collection ratio every five seconds`

## 验证结果

### 1. 全量单元测试

命令：

```bash
npm run test:unit -- --run
```

结果：

- Test Files：10 passed
- Tests：118 passed
- 退出码：0

### 2. 生产构建

命令：

```bash
npm run build
```

结果：

- Vite 成功构建。
- 转换模块数：709 modules transformed。
- 退出码：0。
- 构建输出提示部分 chunk 大于 500 kB，这是项目既有体积告警，不是本阶段新增构建失败。

### 3. 指定 3D 场景截图

命令：

```bash
npm run scene:snap -- --label principle-animation-after-fix --zoom 1.2 --wait 3000
```

结果：

- 退出码：0。
- 截图文件：`artifacts/scene/captures/principle-animation-after-fix.png`
- sha256：`a2ba2aed4689dd3878a5bdfbc08ae61da1484cf06f0f3e7fc877c8c117e0ebe5`
- URL：`http://127.0.0.1:4173/3d-view?diagnostic=1&zoom=1.2&wait=3000`
- 诊断状态：
  - `ready: true`
  - `error: null`
  - `objectCount: 70`
  - `visibleObjectCount: 70`
  - `left_plane`、`right_plane` 均为 `已绑定 canvas 纹理` 且可见。

### 4. 视觉检查

命令：

```bash
npm run vis:check
```

结果：

- 退出码：0。
- 当前截图：
  - `home-desktop`
  - `home-mobile`
  - `3d-overview`
  - `3d-zoom-close`
  - `3d-mobile`
- 3D 诊断截图均显示：
  - `ready: true`
  - `objectCount: 70`
  - `visibleObjectCount: 70`
- 基线状态：
  - `baselineExists: true`
  - `missing: []`
  - `added: []`
  - `changed` 包含 5 张截图：`home-desktop.png`、`home-mobile.png`、`3d-overview.png`、`3d-zoom-close.png`、`3d-mobile.png`

## 视觉差异说明

`npm run vis:check` 报告当前截图哈希与历史 baseline 不同，但命令退出码为 0，且没有缺失或新增截图项。考虑到本轮任务修改了原理动画、统计面板文案、电场线绘制、轨迹生命周期和 3D 小窗刷新触发字段，视觉哈希变化属于需要人工确认或后续更新 baseline 的预期结果。

本阶段未执行 `vis:baseline`，因为更新视觉基线会改变项目基线资产，应由用户确认后单独进行。

## 交付状态

- 模式切换即时刷新：已有单测覆盖 Canvas watcher key。
- 原理动画默认电压：已有组件单测覆盖 `-1 V`。
- 伏安轨迹生命周期：已有引擎单测覆盖 `1.5 s`。
- 电场线方向和密度：已有物理、映射、引擎纯函数测试覆盖。
- 讲解浮窗：已有组件单测覆盖非阻塞浮窗与参数响应式更新。
- 5 秒收集率：已有引擎和面板单测覆盖。
- 全量单元测试、构建、场景截图、视觉检查均已运行。

## 未解决风险

- 视觉基线已出现 5 张截图哈希变化，但尚未进行人工视觉验收或基线更新。
- 生产构建仍存在大 chunk 告警；这不是本阶段新增问题，但后续若关注加载性能，应单独拆分 chunk 或调整构建策略。

## 建议下一步

- 由人工打开 `artifacts/scene/captures/principle-animation-after-fix.png` 和 `artifacts/visual/current/*.png` 检查视觉结果。
- 若确认视觉变化符合预期，再单独运行 `npm run vis:baseline` 更新视觉基线。
