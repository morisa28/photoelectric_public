# 阶段文档21-2-new(26)-伏安轨迹生命周期调整

生成时间：2026-06-05  
对应阶段：阶段 B  
提交目标：将伏安特性模式下电子轨迹点生命周期从 `4 s` 缩短为 `1.5 s`。

## 完成内容

- 将 `engine.js` 中 `VOLT_AMP_TRAIL_POINT_LIFETIME_SECONDS` 从 `4` 改为 `1.5`。
- 同步更新伏安轨迹归档、点裁剪、剩余生命周期、淡出透明度的测试期望。
- 将相关测试描述从“四秒生命周期”改为“一点五秒生命周期”。
- 缩短测试中的中途检查和最终消失时间窗口，避免测试仍按旧 4 秒口径等待。

## 改动文件

- `src/features/photoelectric-animation/engine.js`
- `src/features/photoelectric-animation/engine.spec.js`

## 验证结果

已执行：

```bash
npm run test:unit -- --run src/features/photoelectric-animation/engine.spec.js
```

结果：

```text
Test Files  1 passed (1)
Tests       59 passed (59)
```

## 未完成 / 风险

- 本阶段只调整伏安轨迹生命周期，没有改变电子飞行动力学、收集率、电场线或讲解浮窗。
- 轨迹视觉会明显更快淡出，后续阶段 F 仍需通过截图或人工观察确认整体视觉节奏符合预期。

## 下一阶段建议

- 进入阶段 C：重构电场线方向与密度规则，保证电场线方向与电子受力方向相反，并按电压绝对值动态调整间距。
