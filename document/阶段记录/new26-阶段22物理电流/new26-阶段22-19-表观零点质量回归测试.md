# 阶段文档22-19-new(26)-表观零点质量回归测试

## 阶段目标

按前一阶段数值核对结论，将“阳极寄生与阴极电流提前抵消形成的表观零电流点，不应被当作高可信截止点”固化为单元测试。本阶段不改物理公式、不改 UI、不改拟合逻辑，只增加测试覆盖。

## 完成内容

- 修改 `src/stores/experiment.spec.js`。
- 新增回归测试：
  - 场景为 Cs、365 nm。
  - 外加电压设置为约 `-1.2955 V`，该点接近当前默认阳极寄生下的物理测量电流表观零点。
  - 该点物理测量电流接近 0，但真实阴极电流仍大于 0，阳极寄生电流为负。
  - 测试要求该记录的 `cutoffQuality` 为 `suspect`，不能误判为 `valid`。

## 验证结果

```bash
npm run test:unit -- --run src/features/experiment/photoelectricModel.test.js src/stores/experiment.spec.js src/components/__tests__/PrincipleAnimationSection.spec.js src/components/__tests__/ThreeDMicroAnimationPanel.spec.js
```

结果：

- 4 个测试文件通过。
- 34 个测试用例通过。

## 影响范围

- 只新增测试覆盖。
- 不修改源码物理计算。
- 不修改记录口径。
- 不修改 UI。
- 不暂存或提交用户提供的两份原始指导文档。
