# 阶段文档22-16-new(26)-当前代码适配版电流逻辑方案

## 阶段目标

用户已确认 14 项电流逻辑修正方向，本阶段根据这些方向整合 `photoelectric_current_logic_fix_guide.md` 与 `current_version_current_logic_fix_guide.md`，输出第三版“当前代码适配版实施方案”。本阶段只写方案文档，不修改源码。

## 完成内容

- 新增 `document/当前总纲/03-当前代码适配版电流逻辑实施方案-new26.md`。
- 方案明确：
  - 不进行回退。
  - 只聚焦截止电压模式，伏安特性模式尽量不动。
  - 保留当前非包围式模型。
  - 保留当前 3D 电流屏显示仪器读数的口径。
  - 暗电流、本底电流只保留在原理动画演示，不进入 3D 仪器读数。
  - 不新增自动表观截止电压求解。
  - 不新增 UI 或诊断面板。
  - 实验仪器使用光阑/距离通量模型，原理动画使用百分比光强。
  - 截止记录继续以用户手动外加反向电压为核心，质量合格数据用于拟合。
  - 正常操作下默认截止记录应接近理论值；若明显偏离，应从实验设计、记录判定、阳极寄生和操作引导综合排查。
- 方案同时指出两份旧指导文档中可吸收和不应直接采用的部分。

## 验证结果

本阶段只新增 Markdown 文档，源码未改动。提交前仍运行相关物理和展示链路单元测试。

```bash
npm run test:unit -- --run src/features/experiment/photoelectricModel.test.js src/stores/experiment.spec.js src/components/__tests__/PrincipleAnimationSection.spec.js src/components/__tests__/ThreeDMicroAnimationPanel.spec.js
```

结果：

- 4 个测试文件通过。
- 33 个测试用例通过。

## 影响范围

- 新增当前代码适配版实施方案文档。
- 新增本阶段文档。
- 不修改源码、UI、测试或依赖。
- 不暂存或提交用户提供的两份原始指导文档。
